% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_efa_trail.R
\name{plot.efa_trail}
\alias{plot.efa_trail}
\title{Plot the KMO trajectory across purification iterations}
\usage{
\method{plot}{efa_trail}(x, ...)
}
\arguments{
\item{x}{An object of class \code{"efa_trail"}, as returned by
\code{\link{efa_trail}}.}

\item{...}{Further graphical parameters (currently unused).}
}
\value{
Invisibly returns \code{x}. Called for its plotting side effect.
}
\description{
For every construct in an \code{efa_trail} object, plots the overall
KMO/MSA value at each purification iteration, so the effect of iterative
item removal on sampling adequacy is visible at a glance. One panel is
drawn per construct, side by side.
}
\examples{
\dontrun{
result <- efa_trail(synthetic_reading, list(
  SA = grep("^SA", names(synthetic_reading), value = TRUE),
  CS = grep("^CS", names(synthetic_reading), value = TRUE)
))
plot(result)
}
}
