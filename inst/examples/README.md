# Example run output

`efaTrail_iteration_summary.csv` and `efaTrail_final_summary.csv` in this
folder are the actual output of running `efa_trail()` on `synthetic_reading`
(see the package README for the exact code). They are included as a
reference / supplementary-material example, not used by the package code.
